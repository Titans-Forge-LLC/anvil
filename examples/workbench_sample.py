"""Small editing exercise, not a performance benchmark.

Ask the workbench to return zero for empty input while preserving other cases.
"""


def average(values):
    return sum(values) / len(values)
